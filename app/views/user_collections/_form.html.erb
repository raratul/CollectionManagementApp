<%= form_with(model: user_collection) do |form| %>
  <% if user_collection.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(user_collection.errors.count, "error") %> prohibited this user_collection from being saved:</h2>

      <ul>
        <% user_collection.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.label :user_id, style: "display: block" %>
    <%= form.text_field :user_id %>
  </div>

  <div>
    <%= form.label :collection_id, style: "display: block" %>
    <%= form.text_field :collection_id %>
  </div>

  <div>
    <%= form.submit %>
  </div>
<% end %>
