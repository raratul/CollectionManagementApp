<div class="container mt-3">
  <h2 class="mb-4 text-center">Log in</h2>

  <%= simple_form_for(resource, as: resource_name, url: session_path(resource_name), html: { class: 'needs-validation', novalidate: true }) do |f| %>
    <% if resource.errors.any? %>
      <div class="alert alert-danger">
        <h4 class="alert-heading"><%= pluralize(resource.errors.count, "error") %> prohibited this user from being saved:</h4>
        <ul>
          <% resource.errors.full_messages.each do |message| %>
            <li><%= message %></li>
          <% end %>
        </ul>
      </div>
    <% end %>

    <div class="form-group">
      <%= f.input :email,
                  required: false,
                  autofocus: true,
                  input_html: { autocomplete: "email", class: "form-control" },
                  wrapper_html: { class: "mb-3" } %>
    </div>

    <div class="form-group">
      <%= f.input :password,
                  required: false,
                  input_html: { autocomplete: "current-password", class: "form-control" },
                  wrapper_html: { class: "mb-3" } %>
    </div>

    <br>

    <div class="form-group form-check">
      <%= f.input :remember_me, as: :boolean, wrapper_html: { class: "form-check-label" } if devise_mapping.rememberable? %>
    </div>

    <br>

    <div class="form-group">
      <%= f.button :submit, "Log in", class: "btn btn-primary btn-block" %>
    </div>
  <% end %>

  <br>

  <%= render "devise/shared/links" %>
</div>
