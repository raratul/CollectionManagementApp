<h1 class="d-flex justify-content-center mt-3">Admin Page</h1>

<%= form_with(url: batch_action_admin_path, method: :patch) do |f| %>
  <div class="mt-3 mb-5">
    <%= submit_tag "Make Admin", class: "btn btn-outline-success me-3 mt-3", name: "add_admins" %>
    <%= submit_tag "Remove Admin", class: "btn btn-outline-secondary me-3 mt-3", name: "remove_admins" %>
    <%= submit_tag "Unblock", class: "btn btn-outline-info me-3 mt-3", name: "unblock_users" %>
    <%= submit_tag "Block", class: "btn btn-outline-warning me-3 mt-3", name: "block_users" %>
    <%= submit_tag "Delete", class: "btn btn-outline-danger me-3 mt-3", name: "delete_users", data: { confirm: "Are you sure?" } %>  
  </div>
  <div class="table-responsive">
    <table class="table table-striped table-hover">
      <thead>
        <tr>
          <th>User ID</th>
          <th>Name</th>
          <th>Email</th>
          <th>Last Login Time</th>
          <th>Admin/User</th>
          <th>Blocked/Active</th>
          <th>Select</th>
        </tr>
      </thead>
      <tbody>
        <% @users.each do |user| %>
          <tr>
            <td><%= user.id %></td>
            <td><%= user.name %></td>
            <td><%= user.email %></td>
            <td><%= user.current_sign_in_at %></td>
            <td><%= user.admin? ? 'Admin' : 'User' %></td>
            <td><%= user.blocked? ? 'Blocked' : 'Active' %></td>
            <td><%= check_box_tag "user_ids[]", user.id %></td>
          </tr>
        <% end %>
      </tbody>
    </table>
  </div>  
<% end %>
